{
  "name": "lms-team-shiksha",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/backend\" \"npm run dev --prefix apps/frontend\"",
    "build": "npm run build --prefix apps/frontend",
    "start": "npm run start --prefix apps/backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
